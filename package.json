{
	"name": "workspace",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "pnpm --filter web run dev",
		"build": "pnpm --filter web run build",
		"test": "pnpm run --recursive test",
		"check": "pnpm run --recursive check",
		"lint": "pnpm run --recursive lint",
		"android": "pnpm --filter web run android"
	},
	"type": "module"
}